import { Brand } from '@/constants/theme';

export const APP_NAME = Brand.name;

export const UX_RULES = {
  primaryActionPerScreen: 1,
  minimumTouchTarget: 44,
  mobileMaxContentWidth: 640,
} as const;
